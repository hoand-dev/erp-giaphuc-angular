const HOST = '113.161.95.205';
const PORT = '88';

export const environment = {
    production: true,
    version: "2021.04.20",
    apiUrl: `http://${HOST}:${PORT}/api`,
    baseUrl: `http://${HOST}:${PORT}`,
};